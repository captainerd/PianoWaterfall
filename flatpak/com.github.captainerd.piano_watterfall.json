{
  "app-id": "com.github.captainerd.piano_waterfall",
  "runtime": "org.freedesktop.Platform",
  "runtime-version": "25.08",
  "sdk": "org.freedesktop.Sdk",
  "sdk-extensions": ["org.freedesktop.Sdk.Extension.rust-stable"],
  "command": "piano-waterfall",
  "no-appstream-compose": true,
  "finish-args": [
    "--share=ipc",
    "--socket=fallback-x11",
    "--socket=wayland",
    "--socket=pulseaudio",
    "--device=dri"
  ],
  "build-options": {
    "append-path": "/usr/lib/sdk/rust-stable/bin"
  },
  "modules": [
    {
      "name": "piano_waterfall",
      "buildsystem": "simple",
      "build-commands": [
        "cargo --offline build --release",
        "mkdir -p /app/share/piano_waterfall/",
        "cp -ap ./default.sf2 /app/share/piano_waterfall/default.sf2",
        "find target/release -maxdepth 1 -type f -executable -exec install -Dm755 {} -t /app/bin/ \\;",
        "install -Dm644 ./flatpak/com.github.captainerd.piano_waterfall.desktop -t /app/share/applications/",
        "install -Dm644 ./flatpak/com.github.captainerd.piano_waterfall.png -t /app/share/icons/hicolor/256x256/apps/"
      ],
      "sources": [
        {
          "type": "dir",
          "path": "../"
        }
      ]
    }
  ]
}