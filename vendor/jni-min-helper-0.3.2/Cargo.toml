# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "jni-min-helper"
version = "0.3.2"
authors = ["wuwbobo2021 <wuwbobo@outlook.com>"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = """
Minimal helper for `jni-rs`, supporting dynamic proxies, Android dex embedding,
permission request and broadcast receiver. Used for calling Java code from Rust.
"""
readme = "README.md"
keywords = [
    "java",
    "android",
    "proxy",
    "invocation",
]
categories = ["api-bindings"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/wuwbobo2021/jni-min-helper"

[package.metadata.docs.rs]
default-target = "aarch64-linux-android"
features = [
    "proxy",
    "futures",
]
targets = [
    "aarch64-linux-android",
    "x86_64-apple-darwin",
    "x86_64-pc-windows-msvc",
    "x86_64-unknown-linux-gnu",
]

[features]
default = ["proxy"]
futures = [
    "proxy",
    "dep:futures-core",
    "dep:futures-timer",
    "dep:futures-channel",
    "dep:atomic-waker",
    "dep:futures-lite",
]
proxy = []

[lib]
name = "jni_min_helper"
path = "lib.rs"

[build-dependencies.android-build]
version = "0.1.2"

[target.'cfg(not(target_os = "android"))'.dependencies.jni]
version = "0.21.1"
features = ["invocation"]

[target.'cfg(target_os = "android")'.dependencies.atomic-waker]
version = "1.1"
optional = true

[target.'cfg(target_os = "android")'.dependencies.futures-channel]
version = "0.3"
optional = true

[target.'cfg(target_os = "android")'.dependencies.futures-core]
version = "0.3"
optional = true

[target.'cfg(target_os = "android")'.dependencies.futures-lite]
version = "2.6"
optional = true

[target.'cfg(target_os = "android")'.dependencies.futures-timer]
version = "3.0"
optional = true

[target.'cfg(target_os = "android")'.dependencies.jni]
version = "0.21.1"

[target.'cfg(target_os = "android")'.dependencies.log]
version = "0.4"

[target.'cfg(target_os = "android")'.dependencies.ndk-context]
version = "0.1"
